# The raw uploaded design library — hundreds of MB of photos and video that must
# never be pushed. Designs are uploaded into the app itself now (and travel with
# the workspace / Drive backup), so nothing here is needed to run it.
.sample-preview/
samples/assets/

# build / preview scratch
.preview-out/
.clone-preview/
_preview/
.import-tmp/
.serve.log
.serve2.log
.fa.css
_t.js

# agent scratch
.freebuff/

# editor + OS noise
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store

# node
node_modules/
npm-debug.log*
